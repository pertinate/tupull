import React from 'react';

interface Props {
    title: string;
}

function PollCreationItem(props: Props) {
    const { } = props;

    return (
        <div>
            hello
        </div>
    );
}

export default PollCreationItem;
