import React from 'react';

interface Props { }

function About(props: Props) {
    const { } = props;

    return (
        <div>
            my about page
        </div>
    );
}

export default About;
